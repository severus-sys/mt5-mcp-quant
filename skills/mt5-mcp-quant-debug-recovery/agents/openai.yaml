interface:
  display_name: "MT5 Debug and Recovery"
  short_description: "MT5 hata, çökme ve takılmalarını teşhis eder"
  default_prompt: "Use $mt5-mcp-quant-debug-recovery to diagnose why my MT5 job failed or became stuck."
dependencies:
  tools:
    - type: "mcp"
      value: "mt5_mcp_quant"
      description: "Native Windows MT5-MCP-Quant MCP server"
policy:
  allow_implicit_invocation: true
