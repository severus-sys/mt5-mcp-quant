interface:
  display_name: "MT5 Backtesting"
  short_description: "Native MT5 backtestlerini çalıştırır ve izler"
  default_prompt: "Use $mt5-mcp-quant-backtesting to run and verify a native Windows MT5 backtest."
dependencies:
  tools:
    - type: "mcp"
      value: "mt5_mcp_quant"
      description: "Native Windows MT5-MCP-Quant MCP server"
policy:
  allow_implicit_invocation: true
