interface:
  display_name: "MT5 Calendar Data"
  short_description: "Canlı takvimi statik tester verisine dönüştürür"
  default_prompt: "Use $mt5-mcp-quant-calendar-data to export broker calendar events and prepare a checked Strategy Tester dataset."
dependencies:
  tools:
    - type: "mcp"
      value: "mt5_mcp_quant"
      description: "Native Windows MT5-MCP-Quant MCP server"
policy:
  allow_implicit_invocation: true
