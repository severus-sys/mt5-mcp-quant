interface:
  display_name: "MT5 Optimization"
  short_description: "MT5 parametre optimizasyonunu uçtan uca yönetir"
  default_prompt: "Use $mt5-mcp-quant-optimization to prepare and run a bounded model-0 optimization."
dependencies:
  tools:
    - type: "mcp"
      value: "mt5_mcp_quant"
      description: "Native Windows MT5-MCP-Quant MCP server"
policy:
  allow_implicit_invocation: true
